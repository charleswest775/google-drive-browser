{
  "name": "google-drive-browser",
  "version": "1.0.0",
  "description": "Browse, download, and delete all Google Drive files including orphans",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "install-backend": "cd backend && pip install -r requirements.txt"
  },
  "dependencies": {
    "electron": "^28.0.0"
  },
  "devDependencies": {}
}
